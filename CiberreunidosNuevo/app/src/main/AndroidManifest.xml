<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ciberreunidossa.ciberreunidos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".pant_ppal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".piedrapapeltijera" />
        <activity android:name=".pant_ajustes" />
        <activity android:name=".juego_piedrapapeltijera"></activity>
        <activity android:name=".pant_ahorcado" />
        <activity android:name=".pant_enraya"></activity>
    </application>

</manifest>
